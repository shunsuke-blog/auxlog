# 要件定義書 - Auxlog（9週間プログラム管理・コーチング可視化プラットフォーム）

## ⚡ 方向転換について（2026-07-03 doc sync）

このドキュメントは元々「筋トレメニュー自動提案アプリ」として書かれたが、2026-06-26〜28の開発でプロダクトの方向性が転換した。以下、齟齬防止のため現状を明記する。

- **現在のAuxlogの定義**: 9週間プログラム（Volume→Intensity→Deload→MaxOut）管理・コーチング可視化プラットフォーム。詳細ロジックは `program-based-logic-design.md` を参照。
- **旧ロジックの扱い**: `program-based-logic-design.md` §13 では「`lib/engine.ts` を全面書き換え・旧ロジックは削除」と計画されていたが、**実装では旧ロジック（`lib/suggest/engine.ts` の適応型提案）は削除されず現存**しており、`/api/suggest`（`app/(app)/record/page.tsx`の種目追加フロー）から今も呼ばれている。つまり以下の §2.3 に書かれた「前回記録から次回メニューを提案する」ロジックは**廃止済みではなく、"記録画面での種目追加候補の算出にのみ使う二次的ロジック"に降格**している。**§2.4（ホーム画面）で説明しているスワイプ式のメニュー提案UI自体は2026-07-09に削除済み**（エンロール必須化により到達不能コードになっていたため。`components/home/HomeMenu.tsx`等）。現在のホーム画面は`ProgramDayView`（§2.7参照）のみで、プログラム外提案ロジックのUIは存在しない。新規に画面や文言を作る際は、対象がプログラム経由の種目かどうかをまず確認すること。
- **背景**: この製品転換は事業計画（コーチング事業とAuxlogの一体化）に伴うもの。詳細は `.company/coaching/coaching_business_plan.md` §0・§7、および `.company/ceo/decisions/2026-07-01-business-plan-update-auxlog-coaching.md` を参照。
- **プログラム内部ロジックの再刷新（2026-07-08）**: `program-based-logic-design.md`が説明する旧スロット方式（`program_slots.ts`）はさらに canonical順位ベースのカテゴリ方式（`program_composition.ts`）へ全面置き換えされた。現在の一次情報源は`program-composition-redesign-brainstorm.md`。詳細は§2.7参照。

---

## 1. プロダクト概要

### プロダクト名
Auxlog

### コンセプト
**「9週間のプログラムに沿って記録するだけで、進捗が可視化されるトレーニング管理アプリ」**

中級者・上級者向けの、研究ベースの9週間ピリオダイゼーションプログラム（Volume→Intensity→Deload→MaxOut）を軸にしたトレーニング記録・進捗可視化Webアプリ。コーチングサービスと一体で提供され、クライアントは記録するだけでプログラム進捗・フェーズ説明・重量推移グラフを確認できる。
プログラムに含まれない種目（ユーザーが独自に追加した種目など）については、前回記録から次回の重量・回数を提案する旧ロジック（下記§2.3）が引き続きフォールバックとして動作する。

### ターゲットユーザー
- 筋トレ歴1年以上の中級者・上級者
- やる種目はある程度決まっているが、セット設計で伸び悩んでいる or 面倒と感じている人
- ジムに週2〜5回通っている社会人

### 解決するペイン
- ジムに行ってから「今日何をやるか」を考えるのが面倒
- 感覚に頼るとベンチプレスなど好きな種目に偏る
- 前回の記録を覚えておらず、重量・回数の増減判断ができない

---

## 2. 機能要件

### フェーズ1（実装済み）

#### 2.1 種目登録・編集（⚡ 2026-07-12更新: 現在の`/exercises`画面は追加・削除のみ。実態に合わせて修正）
- システム側があらかじめ一般的な種目マスタを保持している
- ユーザーはマスタの中から「自分がやる種目」を選ぶだけ（タップで完了）
- 種目名・部位の手入力は不要
- マスタにない種目は独自種目として追加できる（その場合のみ名前・部位・種目タイプ（アイソレーション/コンパウンド）を入力）
- 登録済み種目は部位別にグループ表示され、削除のみ可能（削除すると紐づく記録も削除される）
- 標準セット数・標準レップ数（`default_sets`/`default_reps`）はDB上のカラムとして存在するが、`/exercises`画面から編集するUIは無い（追加時にデフォルト値が設定されるのみ）
- 表示順（`sort_order`）のドラッグ並び替えUIは無い（追加順）
- 自重種目フラグ（`is_bodyweight`）を持つ（懸垂等の加重オプション表示に使用）
- コンパウンド種目フラグ（`is_compound`）を持つ（回復日数の計算に使用）

**システム種目マスタ（⚡ 2026-07-12更新: 実データに合わせて例を更新。全81種目・スキーマは§6参照）**

以下はtier1（最もデフォルト推奨）の代表例。全種目は`exercise_master`テーブル参照（2026-07-10時点で81種目、部位×動きパターンの組み合わせで網羅）。

| 種目名 | 対象部位 | 動きパターン |
|--------|---------|------------|
| バーベルベンチプレス | 胸 | horizontal_press |
| ダンベルフライ / ケーブルフライ | 胸 | shoulder_horizontal_adduction |
| ハイバースクワット | 脚 | squat |
| デッドリフト | 背中 | hip_hinge |
| ベントオーバーロウ / チェストサポーテッドロウ | 背中 | horizontal_pull |
| 懸垂 / ラットプルダウン | 背中 | vertical_pull |
| バイキングプレス | 肩 | vertical_press |
| ケーブルサイドレイズ | 肩 | shoulder_abduction |
| バーベルカール | 腕 | elbow_flexion |
| トライセプスプレスダウン | 腕 | elbow_extension |
| アブローラー | 体幹 | trunk_flexion |

#### 2.2 トレーニング記録の入力
- トレーニング実施日（日付変更可能、過去日付に遡って入力可）
- 実施した種目
- 各種目の重量・セット数・レップ数
- ウォームアップフラグ（W）: セットをウォームアップとしてマーク
- 実施フラグ（done）: セットを実際に行ったかのチェック。ONのセットのみ保存
- 主観的余裕度（RIR）: 限界 / 余裕あり の2択トグル（ウォームアップ時は非表示）
- 主観的疲労度（1〜5のスケール）
- メモ（任意）
- 自重種目は加重をオプション表示（「+ 加重」ボタンで表示切替）
- RIRのデフォルトは「限界」（false）

#### 2.3 次回メニューの自動提案（⚡ 現在はプログラム外種目のみのフォールバックロジック。主力機能は §2.7 参照）
以下のルールベースロジックで提案する（MVPはAI不要）

**トレーニングレベル**
ユーザーのトレーニング歴に応じて週ボリューム目標を個人化する。ユーザーの大半が中級者であるため、
設定画面での変更UIは廃止した（2026-07-11）。`training_level`カラムはDB上に残り、デフォルト値
`intermediate`のままロジックは動き続ける。

| レベル | 目安 | 週目標セット数（筋群ごと） |
|--------|------|--------------------------|
| 初級者 | 1年未満 | 8〜12セット |
| 中級者 | 1〜3年 | 12〜16セット |
| 上級者 | 3年以上 | 16〜20セット |
根拠：筋肥大研究の最新メタアナリシス（Schoenfeld et al. 2021、RP Strength 2025、PubMed 2025）に基づく

**種目選択ロジック**
- 前回トレーニングからの経過日数が長い種目を優先（降順ソート）
- 前回のセット内容・種目タイプに応じた最低回復日数を下回る種目は除外
  - コンパウンド種目 × 限界セットあり → 3日間除外
  - アイソレーション種目 × 限界セットあり → 2日間除外
  - 全セット余裕あり（RIR=true）→ 2日間除外
  - 初回（前回記録なし） → 常に提案
- ~~7日以上経過した種目は「優先」としてアクセントカラーボーダーで視覚的に強調~~（⚡ 2026-07-12確認: 表示していた`SwipeableExerciseCard`は2026-07-09削除済みのため、現在このロジックを表示するUIは無い。`days_since_last`自体はエンジン内で今も計算される）
- ユーザーが登録している種目の中からのみ提案

**重量・セット提案ロジック**
プログレッシブオーバーロードの優先順位に従い、以下の順で判定する

| 状況 | 提案 |
|------|------|
| 初回（記録なし） | default_sets × default_reps の直線セット |
| 疲労度 >= 4（有酸素種目） | 重量 × 0.95（2.5kg丸め）、レップ数維持 |
| 疲労度 >= 4（自重種目） | レップ数 × 0.8（20%減） |
| トップセットがレップ未達 | 重量維持、目標レップを前回+1回 |
| 全トップセットが余裕あり・レップ達成（有酸素） | 重量 +2.5kg、default_reps でリセット |
| 全トップセットが余裕あり・レップ達成（自重） | レップ数 +2回 |
| 3セッション停滞（ストール）| セット数 +1 |
| ギリギリ達成 | 重量・レップ数維持 |

**判定の基準**
- 判定にはウォームアップセットを除外したワーキングセットのみ使用
- トップセット（最大重量のセット）のみでRIR・レップ判定を行う
- 前回の各セット重量パターンを引き継ぐ（ピラミッド・逆ピラミッド対応）
- ストール判定はレップ達成時のみ適用（未達は進捗中のため除外）

**週ボリューム管理ロジック（⚡ 2026-07-05 表示ウィジェットは削除済み）**
- 履歴画面に「週間ボリューム達成率」として各筋群の週セット数を表示していたが、
  固定目標値がプログラムの週次パラメータ・頻度（週2/3/4日）と一切連動しておらず、
  部位タグの不一致（フェイスプルが「背中」判定など）とも相まって実態と乖離した
  表示になっていたため、プログラムベース運用が定着した現在は不要と判断し削除した
  （`components/history/MuscleSetChart.tsx`）。以下は削除前の設計メモ。
- ~~各筋群の週セット数を自動集計して表示~~
- ~~最適範囲：週10〜20セット/筋群（中級者・上級者の筋肥大に有効な範囲）~~
- ~~週セット数が10未満 → ボリューム不足インジケーターを表示~~
- ~~週セット数が20超 → オーバートレーニング注意インジケーターを表示~~

**RIRの入力方法**
- 各セットの余裕度をトグルで入力（2択）
  - 限界（RIR0〜1：あと1回もできない状態）→ 赤系
  - 余裕あり（RIR2以上：まだ余裕がある状態）→ 緑系
- デフォルトは「限界」（false）
- タップ1回で切り替え
- ウォームアップセットはRIRトグルを非表示

#### 2.4 ホーム画面（⚡ 2026-07-09更新: 旧スワイプ式提案UIは削除済み、以下が現行仕様）
エンロール（9週間プログラム登録）は必須のため、ホーム画面は`ProgramDayView`（`components/home/ProgramDayView.tsx`）のみで構成される。旧来の「前回記録から提案」ベースのスワイプUI（`HomeMenu.tsx`等）は到達不能コードだったため削除済み（詳細は冒頭の方向転換セクション参照）。
- Dayセレクターで曜日構成（Day1〜4等）を切り替え
- 「今週のフォーカス」カードで現在のフェーズ（Volume/Intensity/Deload/MaxOut）を説明
- その日のプログラムスロットごとに種目カードを表示。各カードをスワイプ左で「今日はやらない」として非表示にできる（`is_hidden`をDBに保存、当日限定ではなく永続）
- 「+ 種目を追加」ボタンでプログラム外の追加種目をその日のメニューに追加できる
- 週の全スロット完了で「Week N 完了 → Week N+1 へ」ボタンがコンテンツ最上部に表示される（2026-07-09、下部で気付きにくいとのフィードバックにより移動）

#### 2.5 トレーニング履歴の閲覧
- 週次カレンダー（WeekCalendar）で日付を選択してセッション絞り込み
- 月次カレンダーモーダル（MonthCalendar）でより遠い日付に移動可能
- 種目ごとの重量推移グラフ（最大重量・総挙上量・推定1RM の3メトリクス切り替え）
- 過去のトレーニングログ一覧（日付・疲労度・種目・セット詳細）
- 同一日に複数セッションが存在する場合はまとめて1件として表示・編集
- セッション全体の編集ボタン（`/record/edit/[sessionId]`）
- 種目ごとの編集ボタン（`/record/edit/[sessionId]?exerciseId=[id]`）
- セッションの削除機能
- 自重種目のボリュームは回数（rep数）で表示
- 有酸素種目のボリュームはkg（重量×回数）で表示

#### 2.6 記録編集
- 過去のトレーニングセッションを編集できる（`/record/edit/[sessionId]`）
- 種目を絞り込んで特定種目のみ編集できる（`?exerciseId=[id]` クエリ）
- 全体編集モードでは種目の有効/無効トグル（CircleCheck）・種目追加モーダルが使用可能
- 同一日に複数セッションがある場合は `?merge=id1,id2` でまとめて編集し保存時に統合
- セッション削除機能（確認ダイアログあり）
- RPC関数（`update_session_with_sets`）によるアトミック更新

#### 2.7 9週間プログラム機能（⚡ 現在の主力機能、2026-06-28〜実装。⚡ 2026-07-12更新: 2026-07-08の種目選定ロジック刷新に合わせて内容を修正）
研究ベースの9週間ハイパートロフィープログラムをAuxlogに組み込んだもの。前回記録からの適応提案（§2.3）ではなく、プログラムがあらかじめ決めた週次パラメータに沿って重量・レップを算出する。種目選定・日数配分の一次情報源は`program-composition-redesign-brainstorm.md`（`program-based-logic-design.md`・`program-frequency-variants-design.md`が説明する旧スロット方式は2026-07-08に全面置き換え済み）。

- **4フェーズ構成**: W1〜W4 Volume Progression → W5〜W7 Intensity → W8 Deload → W9 Max Out（AMRAP）
- **種目数は日数(2/3/4)で決まる**。6つの基本の動き（ホリゾンタルプレス・バーチカルプレス・ホリゾンタルロウ・バーチカルプル・スクワット・ヒップヒンジ）は日数に関わらず必ず含まれ、日数が増えるほどアクセサリー種目が追加される（canonical順位1-24のカットオフ方式、`lib/suggest/generate_program_composition.ts`）。週2日では`shoulder_press`・`leg_default`・`leg_2`カテゴリのみ1RM管理を強制的にオフにする（BIG3限定ではなく、種目ごとの`requires_one_rm`属性が正）
- 1回のセット数は日数ではなくセッション時間（60/75/90分）で決まる。6パターンカテゴリのセット数のみ週次パラメータ（下記）が別途管理する
- コンパウンド種目はユーザーの1RM入力から%RMベースで重量を自動算出。正式な1RMが無い場合は直近ログからEpley式で自動推定する
- レップレンジ指定種目はRPE基準・実施回数を入力する形式
- 週次進行管理（Week/フェーズを手動で進行）
- **コーチングページ（`/coaching`）**: プログラムの全体像・現在地・フェーズ説明・重量推移グラフを表示
- **種目の選び直し**：コーチングページの「種目情報」タブで各スロットをタップすると、そのスロットで選択可能な種目（`movement_pattern`一致、無ければ`target_muscle`一致で絞り込み。一部カテゴリは複数`movement_pattern`のいずれか一致でも可）からその場で選び直せる。選択すると `user_slot_assignments.exercise_id` を更新（`api/program/slot-assignments/[slot_id]` PATCH、`exercise_name` 指定）。1RM・週次進行・過去の記録には影響しない
- **重量ガイド文言**：記録画面のセット一覧上部に「どんな基準で重量を決めればいいか」を平易な言葉で表示する。プログラム経由の種目は目標レップ範囲・強度（RPE相当を「あと◯回はできる、くらいの重さで」等に言い換え）・週次メモ（ディロード週など）から組み立てる（`lib/utils/set_guidance.ts`）。プログラム外種目（フォールバックの旧提案ロジック）は `Suggestion.reason` をそのまま表示する。専門用語禁止ルール（RPE等の生表記禁止）に準拠

### フェーズ2以降（MVP後）

- AIによる提案精度向上（claude-haiku使用）
- 疲労度の自然言語入力（「今日は肩が重い」など）
- 英語対応
- ネイティブアプリ化（iOS/Android）
- プッシュ通知（トレーニング日のリマインド）

---

## 3. デザイン原則

### 絶対に守るルール
このアプリのコアバリューは「開いた瞬間に今日のメニューがわかる」だ。機能追加の判断は常にこの原則に照らして行う。

**追加前に必ず問う3つの質問**
1. この機能はコアバリューを強化するか？
2. この機能がないと誰かがアプリをやめるか？
3. この機能を追加することで、他の機能が使いにくくなるか？

3つ目がYESなら追加しない。1と2がどちらもNOなら追加しない。

**具体的な制約**
- ホーム画面に表示する情報は「今日やる種目・重量・セット数」のみ
- 1回の記録入力は30秒以内に完了できること
- ナビゲーションは最大4項目まで
- 新機能はフェーズ2以降に持ち越す。MVPに追加しない

### 参考にする思想
- 「引き算のデザイン」：迷ったら削る
- 競合（GymworkやGYMLOG AI）が機能を増やしてきたとしても、追いかけない

### UI/UXデザイン方針
**目指す世界観：シンプル・スタイリッシュ・洗練**
- 筋トレに真剣に向き合う人間が使うツールとして、安っぽさを排除する
- 余白を意識的に使い、情報密度を下げる
- フォント・カラー・アイコンに一貫性を持たせる

**カラーシステム**
- ライトモード: 背景 `#ffffff`、テキスト `#000000`、アクセント `#B8CC00`
- ダークモード: 背景 `#0A0A0A`、カード `#141414`（zinc-950相当）、アクセント `#E8FF00`
- アクセントカラーは優先バッジ・強調ボーダーなど限定的に使用

**具体的な指針**
- カラーパレットは3色以内に絞る（メインカラー・背景・アクセント）
- フォントは1〜2種類まで
- アイコンはlucide-reactで統一
- ボタンやカードの角丸・余白・影を統一する（`rounded-3xl`、`shadow-[0_2px_16px...]`）
- アニメーションは最小限（画面遷移・保存完了のフィードバックのみ）
- テキストは太字・サイズの強弱で階層を作り、色で区別しない
- ヘッダーは全画面で `sticky top-0` 固定、高さ統一（`py-5`）

**PWA対応**
- `viewport-fit: cover` による全画面対応
- `env(safe-area-inset-bottom)` でBottomNav・モーダル・固定ボタンをiOSノッチ対応

**判断基準**
「Appleの純正アプリと並べて恥ずかしくないか」を常に問う

---

## 4. 非機能要件

### パフォーマンス
- トップページ（メニュー提案画面）のロードは3秒以内
- 記録入力から保存完了まで1秒以内

### セキュリティ
- Supabase AuthによるGoogle OAuth認証（メール認証は廃止）
- RLS（Row Level Security）でユーザーデータを完全分離
- HTTPS必須
- 全APIにzodによるバリデーションを適用
- APIエラーメッセージは汎用化（内部情報の漏洩を防止）
- セッション取得のlimitはサーバー側で最大100件に強制

### レスポンシブ対応
- スマートフォンファーストで設計（ジムでスマホから使う想定）
- PC・タブレットでも使用可能

---

## 5. 技術スタック

| 領域 | 技術 |
|------|------|
| フロントエンド | Next.js 16（App Router） |
| バックエンド | Next.js API Routes |
| データベース | Supabase（PostgreSQL） |
| 認証 | Supabase Auth |
| バリデーション | zod |
| ホスティング | Vercel |
| AIエンジン（フェーズ2） | Anthropic API（claude-haiku） |
| 決済 | Stripe |

---

## 6. データベース設計

### users（Supabase Authと連携、⚡ 2026-07-12更新: is_free/free_untilを追記）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| email | text | メールアドレス |
| stripe_customer_id | text | Stripe顧客ID |
| stripe_subscription_id | text | StripeサブスクID |
| subscription_status | text | trialing/active/canceling/canceled/past_due |
| trial_ends_at | timestamptz | トライアル終了日時 |
| is_admin | boolean | 管理者フラグ（請求をスキップ、常にactive扱い） |
| is_free | boolean | 無料ユーザーフラグ（ベータテスター等、サブスク不要で全機能利用可） |
| free_until | timestamptz | 無料期限（NULLなら永久無料）。§8参照 |
| training_level | text | beginner/intermediate/advanced（デフォルト: intermediate）。設定画面での変更UIは廃止済み（§2.3参照） |
| created_at | timestamp | 登録日時 |

### exercise_master（システム共通種目マスタ、⚡ 2026-07-10更新: プログラムカテゴリ関連カラムを追記）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| name | text | 種目名 |
| target_muscle | text | 対象部位（chest/back/shoulders/legs/arms/core） |
| movement_pattern | text | 動きパターン（horizontal_press/elbow_flexion等）。プログラムのカテゴリ判定に使用（`program_composition.ts`） |
| tier | integer | 種目の推奨度（1が最もデフォルト推奨、数字が大きいほどバリエーション的）。オンボーディングはtier<=2のみ表示 |
| requires_one_rm | boolean | 1RM管理（%RMベースの重量算出）が必要な種目かどうか。種目単位の属性（カテゴリ単位ではない） |
| intensity_technique | text | 60分プログラムのアイソレーション最終セットに許可する強度テクニック（rest_pause/myo_reps/none） |
| sort_order | integer | 表示順（種目ピッカーでの並び順、部位ブロック→動きパターンのサブブロック→種目の階層的な番号体系） |
| is_bodyweight | boolean | 自重種目フラグ（デフォルトfalse） |
| is_compound | boolean | コンパウンド種目フラグ（デフォルトfalse） |
| slot_type | text | 旧スロット方式の名残。現在のロジックでは未使用（movement_pattern/target_muscleに置き換え済み） |
| created_at | timestamp | 登録日時 |

### user_exercises（ユーザーが選んだ種目）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| user_id | uuid | FK → users.id |
| exercise_master_id | uuid | FK → exercise_master.id（NULLは独自種目） |
| custom_name | text | 独自種目名 |
| custom_target_muscle | text | 独自種目の部位 |
| default_sets | integer | 標準セット数 |
| default_reps | integer | 標準レップ数 |
| sort_order | integer | 表示順 |
| is_active | boolean | 有効フラグ |
| is_bodyweight | boolean | 独自種目の自重フラグ（デフォルトfalse） |
| created_at | timestamp | 登録日時 |

### training_sessions（トレーニングセッション）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| user_id | uuid | FK → users.id |
| trained_at | date | トレーニング実施日 |
| fatigue_level | integer | 疲労度（1〜5） |
| memo | text | メモ（任意） |
| created_at | timestamp | 登録日時 |

### training_sets（各種目の記録）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| session_id | uuid | FK → training_sessions.id |
| exercise_id | uuid | FK → user_exercises.id |
| set_number | integer | セット番号 |
| weight_kg | decimal | 重量（kg） |
| reps | integer | レップ数 |
| rir | boolean | 余裕あり=true / 限界=false |
| is_warmup | boolean | ウォームアップフラグ（デフォルトfalse） |
| created_at | timestamp | 登録日時 |

---

## 7. 画面設計

### 画面一覧
1. **ホーム（メニュー提案画面）** - アプリを開いたら即座に今日のメニューが表示される
2. **記録入力画面** - トレーニング後に結果を入力
3. **記録編集画面** - 過去のセッションを編集・削除
4. **履歴画面** - 週次/月次カレンダーとセッション一覧・グラフ
5. **種目管理画面** - 種目の登録・編集・削除
6. **設定画面** - プロフィール・課金管理
7. **コーチングページ（`/coaching`）** - 9週間プログラムの全体像・現在地・フェーズ説明・重量推移グラフ（種目選定ロジックの詳細は `program-composition-redesign-brainstorm.md` 参照。§2.7参照）

### ホーム画面の設計思想
- アプリを開いた瞬間に今日やるべき種目・重量・セット数が表示される（SSR）
- カードをスワイプして「今日はやらない」種目を非表示にできる
- 「+」ボタンで種目を追加できる
- 「記録を入力する」ボタンへの導線が明確

---

## 8. マネタイズ設計

### モデル
サブスクリプション（月額課金）＋30日間無料トライアル

### 料金
¥480/月（税込）

### 無料トライアル
- 期間：30日間
- 全機能が使える
- クレジットカード登録不要でトライアル開始（Stripe Subscription: trial_period_days=30）
- 30日後に自動課金開始（要カード登録）、事前通知メールあり（Stripe trial_will_end webhook）

### サブスクリプションステータス
| ステータス | 意味 | アクセス |
|-----------|------|---------|
| `trialing` | トライアル期間中 | 全機能 |
| `active` | 有効（課金中） | 全機能 |
| `canceling` | 解約予定（期間終了まで有効） | 全機能（trial_ends_at まで） |
| `canceled` | 解約済み | 記録閲覧のみ |
| `past_due` | 支払い遅延 | 記録閲覧のみ |

### トライアル終了後
- 課金継続 → 全機能利用可能
- 解約 → アカウントとデータは保持、記録閲覧のみ可能（新規記録・提案機能は停止）
- 再開 → `/subscribe` ページからStripe Customer Portal経由でカード登録・再契約可能

### 管理者アカウント
- `is_admin = true` のユーザーはサブスクリプションチェックをスキップ
- 課金なしで全機能が常時利用可能

### 無料ユーザーアカウント
- `is_free = true` のユーザー（ベータテスター・招待ユーザー等）はサブスクリプション不要で全機能利用可能
- `free_until = NULL` → 永久無料
- `free_until = 日時` → 期限付き無料（期限切れ後は通常のサブスクチェックへ）
- 設定画面にステータスを「無料」または「無料（期限付き）」で表示
- Supabase の users テーブルで直接 `is_free` / `free_until` を設定して管理

### 収益目標との整合性
- 月5万円達成に必要な有料ユーザー数：約105人
- トライアル→有料転換率20%を目標とすると、3ヶ月で525人のトライアル登録が必要

---

## 9. 開発ロードマップ

### フェーズ1（MVP）- 完了
- [x] Supabase・Next.js環境構築
- [x] 認証実装（Google OAuth）
- [x] 種目登録・編集機能（is_compound対応）
- [x] トレーニング記録入力機能
- [x] ルールベースのメニュー提案ロジック実装（コンパウンド/アイソレーション回復日数対応）
- [x] 履歴閲覧・グラフ表示（最大重量/総挙上量/推定1RM切り替え）
- [x] 記録編集・削除機能（多セッション統合・CircleCheck）
- [x] スワイプ削除・種目追加（ホーム画面）
- [x] zodバリデーション
- [x] PWA対応（safe-area）
- [x] Stripe連携（トライアル・解約・再開・再契約・Customer Portal）
- [x] 管理者権限（is_admin）
- [x] www → auxlog.app リダイレクト（middleware.ts）
- [x] 本番デプロイ（Vercel・auxlog.app）
- [x] 9週間プログラム機能（種目選択・1RM管理・週次進行・%RMベース重量算出）（2026-06-28〜、種目選定ロジックは2026-07-08刷新済み。詳細は `program-composition-redesign-brainstorm.md`）
- [x] コーチングページ（`/coaching`：プログラム進捗・フェーズ説明・重量推移グラフ）

### フェーズ2 - 目標：MVP後2ヶ月
- [ ] AIメニュー提案（claude-haiku）
- [ ] 英語対応
- [ ] UX改善（ユーザーフィードバック反映）

### フェーズ3
- [ ] ネイティブアプリ化
- [ ] プッシュ通知

---

## 10. 成功指標

### 3ヶ月後の目標
- 月間アクティブユーザー：100人
- 有料転換率：5%
- 月次売上：¥50,000

### トラッキング指標
- DAU/MAU比率（継続率の指標）
- 記録入力回数/ユーザー（エンゲージメントの指標）
- フリー→有料転換率

---

## 11. 競合との差別化

| 機能 | 本アプリ | Gymwork | GYMLOG AI |
|------|---------|---------|-----------|
| 自分の種目のみ提案 | ✅ | ❌ | ❌ |
| 開いた瞬間にメニュー表示 | ✅ | ❌ | ❌ |
| シンプルな記録入力 | ✅ | △ | △ |
| 中級者・上級者特化 | ✅ | ❌ | ❌ |
| ウォームアップセット管理 | ✅ | △ | △ |
| ピラミッドセット対応 | ✅ | △ | ❌ |
